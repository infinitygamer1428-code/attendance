# attendance

create a folder in desktop named auto attendance or whatever you like.
Then inside,create a folder called dataset were u can upload student photo to recognize.
open vs code > open folder auto atendance > on left side right click > create a file attendance.py > paste code that i given. 
open vs code terminal to install required extensions.
   pip install opencv-python
   pip install face-recognition
   pip install numpy
   pip install pandas
   pip install dlib
   if it shows any error go to https://cmake.org/download/.
download windows 64 installer.
intsll it .after installation is completed,
then run this on vs code terminal = 
python -m pip install --upgrade pip setuptools wheel
it should wrk(wrk ayilaghil yanike ariyila...)
again go through 
     pip install opencv-python
     pip install face-recognition 
     pip install opencv-python numpy pandas
then run py file
python attendance.py

yehhhh sambhavam vanidavum.....!!!!!

     

